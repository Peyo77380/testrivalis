{% extends 'base.html.twig' %}

{% block title %}Vos messages{% endblock %}

{% block body %}
    <h1>Vos messages</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ data_to_convert.id }}</td>
            </tr>
            <tr>
                <th>Texte</th>
                <td>{{ data_to_convert.text }}</td>
            </tr>
            <tr>
                <th>Date de création</th>
                <td>{{ data_to_convert.createdAt ? data_to_convert.createdAt|date('d-m-Y') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_data_to_convert_index') }}">Vers la liste</a>

    <a href="{{ path('app_data_to_convert_edit', {'id': data_to_convert.id}) }}">Modifier</a>

    {{ include('data_to_convert/_delete_form.html.twig') }}
{% endblock %}
